language: node_js
# nodejs版本
node_js: 
    - '6'
 
# Travis-CI Caching
cache:
  directories:
    - node_modules
 
 
# S: Build Lifecycle
install:
  - npm install
 
before_script:
 
# 无其他依赖项所以执行npm run build 构建就行了
script:
  - npm run build
 
after_script:
  - cd ./dist
  - git init
  - git config user.name "GY00156"
  - git config user.email "13253134481@163.com"
  - git add .
  - git commit -m "Update tools"
  - git push --force --quiet "https://${GITHUB_TOKEN}@${GH_REF}" master:master
# E: Build LifeCycle
 
#指定分支，只有指定的分支提交时才会运行脚本
branches:
  only:
    - hexo
env:
 global:
   # 我将其添加到了travis-ci的环境变量中
   - GH_REF: github.com/GY00156/gy00156.github.io.git